BlueX Website (Static)

How to run locally:
- Open index.html in a browser, or
- Serve the folder with any static server.

Example (Python):
  python -m http.server 8000
Then open http://localhost:8000

Notes:
- Logos in /assets are used exactly as provided (no edits).
